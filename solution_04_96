class Solution:
    def numTrees(self, n: int) -> int:
        # dp[i] stores the number of unique BSTs with i nodes
        dp = [1, 1] + [0] * (n - 1)
        
  
           
          
                
      
